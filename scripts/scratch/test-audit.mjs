import { createClient } from '@supabase/supabase-js'; const url = process.env.SUPABASE_URL; const key = process.env.SUPABASE_SERVICE_ROLE_KEY; const c = createClient(url, key, { auth: { autoRefreshToken: false, persistSession: false } }); const r = await c.from('orders').select('id').limit(1); console.log('error:', JSON.stringify(r.error)); console.log('rows:', r.data?.length);
